interface:
  display_name: "Grill Me"
  short_description: "Stress-test plans with critical questions"
  default_prompt: "Use $grill-me to critically examine this plan before implementation."
policy:
  allow_implicit_invocation: true
